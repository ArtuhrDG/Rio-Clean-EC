# 1. Crea el entorno (usa tu instalación de python 3.12)
python -m venv venv
# 2. Activa el entorno en PowerShell
.\venv\Scripts\Activate.ps1

# 3. Instalar dependecias en entorno virtual
pip install -r requirements.txt